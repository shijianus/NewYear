# 🚀 快速部署指南

## 方式一：Cloudflare Pages（推荐）

### 步骤：
1. **访问 Cloudflare**
   - 打开 https://dash.cloudflare.com/
   - 登录你的账号

2. **创建 Pages 项目**
   - 点击左侧 "Workers & Pages"
   - 点击 "Create a project"
   - 选择 "Direct Upload"

3. **上传文件**
   - 将整个 `NewYear` 文件夹拖拽到上传区域
   - 等待上传完成（约 1-2 分钟）

4. **部署完成**
   - 系统会自动部署
   - 成功后你会得到一个类似 `https://your-name.pages.dev` 的链接

## 方式二：GitHub + Cloudflare Pages

### 步骤：
1. **推送到 GitHub**
   ```bash
   git init
   git add .
   git commit -m "星空湖景壁纸"
   git remote add origin <你的仓库地址>
   git push -u origin main
   ```

2. **在 Cloudflare 连接仓库**
   - 在 Cloudflare Pages 中选择 "Connect to Git"
   - 选择你的 GitHub 仓库
   - 使用默认构建设置（无需配置）
   - 点击 "Save and Deploy"

## 方式三：其他静态托管

你也可以部署到：
- Netlify
- Vercel
- GitHub Pages
- Surge.sh

## 验证部署

部署成功后，检查以下几点：
- ✓ 背景图片正常显示
- ✓ 星星闪烁流畅
- ✓ 流星随机出现
- ✓ 鼠标移动有视差效果
- ✓ 移动端正常显示

## 故障排除

### 背景图片不显示
- 检查 wallpaper/output/materials/ 目录是否上传
- 确认文件名是否正确（区分大小写）

### 动画卡顿
- 减少浏览器标签页
- 关闭其他占用 CPU 的程序
- 尝试使用 Chrome/Edge（性能更好）

### 移动端异常
- 检查手机浏览器版本
- 清除浏览器缓存
- 尝试不同的浏览器

## 性能优化建议

如果感觉性能不佳，可以在 `index.html` 中修改：

```javascript
// 减少星星数量（250 → 150）
const stars = Array.from({ length: 150 }, () => new Star());

// 减少流星数量（3 → 1）
const meteors = Array.from({ length: 1 }, () => new Meteor());

// 减少粒子数量（50 → 20）
const particles = Array.from({ length: 20 }, () => new Particle());
```

## 需要帮助？

1. 查看详细文档：[CLOUDFLARE_DEPLOY.md](CLOUDFLARE_DEPLOY.md)
2. 本地测试：`bash preview.sh`
3. 检查浏览器控制台（F12）
4. 查看 Cloudflare 部署日志

---

**祝部署成功！** 🎉
